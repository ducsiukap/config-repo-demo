# Chỉ override những gì khác khi chạy Docker
# docker-compose.yml: SPRING_PROFILES_ACTIVE=docker ./gradlew bootRun
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ms_orders?useSSL=false&allowPublicKeyRetrieval=true

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/